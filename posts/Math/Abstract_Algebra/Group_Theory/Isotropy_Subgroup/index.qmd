---
title: "Isotropy Subgroup"
author: "Youmin Chun"
date: "2026-09:02 00:00:22"
description: "군의 작용(Group Action)이 주어졌을 때 등방부분군(Isotropy Subgroup)과 궤도(Orbit)에 대해서 설명한다."
categories: 
  - Math > Abstract Algebra > Group Theory
---

## Theorem 1
Let $X$ be a $G$-set, and let $g \in G, x \in X.$ We let

$$G_x = \{ g \in G \mid gx = x \} \quad \text{and} \quad X_g = \{ x \in X \mid gx = x \}.$$

Then $G_x \le G,$ and we call it the ***isotropy sugroup*** of $x$.

다시 말해 $G_x$란 $x$를 변화시키지 않는 $g$들의 집합이고, $X_g$란 $g$에 의해 불변인 $x$들의 집합이다. 

### Proof

## Theorem 2
Let $X$ be a $G$-set. For $x_1, x_2 \in X,$ define a relation $\sim$ on $X$ by 

$$x_1 \sim x_2 \iff gx_1 = x_2 \text{ for some } g \in G.$$

Then $\sim$ is an equivalence relation on $X.$ Then an equivalence class under $\sim$ is called an ***orbit in $X$ under $G,$*** and the orbit containing $x$ is called the ***orbit of $x$***, denoted by $Gx.$ That is, $Gx = \{ gx \mid g \in G \}.$

즉 연산을 취했을 때 같은 결과를 주는 $X$의 원소들을 모두 같은 것으로 보겠다는 뜻이고, 그 같은 원소들을 모아놓은 동치류를 orbit이라고 부른다. 

### Proof

# Reference
- John B. Fraleigh. (2004). A First Course in Abstract Algebra (7th ed.). Pearson.