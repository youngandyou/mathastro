{
  "name": "mathastro-likes-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-mathastro --only firestore \"node --test --test-concurrency=1 tests/firebase/*.test.mjs\"",
    "test:browser": "cd ../.. && firebase emulators:exec --project demo-mathastro --only auth,firestore \"node tests/firebase/browser.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.0",
    "playwright": "1.62.1"
  }
}
